{
  "_id": "{{counter()}}",
  "username": "{{chance.name()}}",
  "email": "{{chance.email()}}",
  "firstName": "{{util.sample(['Bob', 'Ted'])}}",
  "lastName": "{{util.sample(['Doe', 'Last'])}}",
  "permissions": {
    "admin": "{{type: Boolean }}"
  }
}
